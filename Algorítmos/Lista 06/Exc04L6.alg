algoritmo "Exc04L6"
// 4)
// Construir um algoritmo que
// Leia um vetor de 15 elementos,
// iniciando a partir do último elemento (15º).
var
n: vetor[1..15] de inteiro
x: inteiro

inicio

para x de 15 ate 1 passo -1 faca
       leia(n[x])
fimpara


fimalgoritmo
